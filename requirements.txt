# Core Dependencies (likely already installed in your vllm environment)
torch>=2.0.0
transformers>=4.40.0
datasets>=2.14.0
tokenizers>=0.15.0
vllm>=0.4.0
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0
sentencepiece>=0.1.99
protobuf>=3.20.0
pyyaml>=6.0
scikit-learn>=1.3.0

# Additional packages you may need to install
accelerate>=0.27.0
fire>=0.5.0
nltk>=3.8.0
rouge-score>=0.1.2

# Optional: For visualizations
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional: For training (LLaMA-Factory)
# Uncomment if you need to run defense training
# peft>=0.10.0
# bitsandbytes>=0.42.0
# trl>=0.7.0
# deepspeed>=0.12.0
# wandb>=0.15.0
